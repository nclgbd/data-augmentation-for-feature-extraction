# from hydra.core.config_store import ConfigStore
# from thesiscore.config import *

# cs = ConfigStore.instance()

# # main configuration
# cs.store(name="cfg", node=Configuration)

# # dataset configurations
# cs.store(name="dataset", group="dataset", node=DatasetConfiguration)
# cs.store(name="tcia-dataset", group="dataset", node=TciaDatasetConfiguration)

# # model configurations
# cs.store(name="torch-model", group="model", node=TorchModelConfiguration)

# # run configurations
# cs.store(name="run", group="run", node=RunConfiguration)
